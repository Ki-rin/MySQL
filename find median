SELECT ROUND(LN, 4) FROM 

(SELECT s1.LAT_N as LN, COUNT(s1.LAT_N) as RNK
FROM STATION s1, STATION s2
WHERE s1.LAT_N < s2.LAT_N OR (s1.LAT_N = s2.LAT_N)
group by s1.LAT_N
order by s1.LAT_N desc) as s3

WHERE RNK = (SELECT (COUNT(*)+1) DIV 2 FROM STATION);
