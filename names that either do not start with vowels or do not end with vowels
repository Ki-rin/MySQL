/* names that either do not start with vowels or do not end with vowels*/

SELECT DISTINCT CITY

FROM STATION 

WHERE SUBSTR(CITY, 1,1) not in  ('A','E','I','O','U','a', 'e', 'i', 'o', 'u')

OR SUBSTR(CITY, -1) not in  ('A','E','I','O','U','a', 'e', 'i', 'o', 'u');
